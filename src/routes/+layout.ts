// The app is a pure SPA: no server-side rendering, no prerendering of dynamic
// routes. `adapter-static` serves `index.html` as the fallback for every path.
export const ssr = false;
export const prerender = false;
